# @inkvas/core

> Inkvas OSS core (Apache-2.0). Phase 3+ implementation.

PDF render + annotation engine + infinite canvas (USP-4) + CRDT (USP-3) + persistence (USP-5) + integrity hash chain (USP-1, OSS측).

- **Status (Phase 1)**: scaffold only
- **First commit (Phase 3 G3)**: `Render a PDF` vertical slice
- **First publish (Phase 5+)**: `npm publish @inkvas/core@0.1.0-alpha`

See repo root `AGENTS.md` + `docs/adr/0001-repo-topology.md` for workspace layout.
